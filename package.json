{
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:desktop": "npm run dev --prefix desktop-shell"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
